Greg Dunlap / celtic_cow

take a file of cma / group names and pulld info out
use this to build a list of zones which contain networks

todo:
input file complete
search program
add policy meta data

completed
meta info for zone
zone build out
basic search test
argparse for extract
add try / except for if a group does not exist


unknown:
zone search for IPv6 host


----

new thread 

use run script to gw to get route table.  comes back as a base64 ... decode script in works
decode script works
moved to main route exact. works on dev.  prod testing going SLOW
